import React from "react";
import "./Alert.css";

const AlertSlider = () => {
  return (
    <div className="alert-slider">
      <div className="scrolling-text">
        The 'Book Appointment' and 'Find Clinic' sections are currently
        hard-coded to demonstrate the interface and functionality. All displayed
        information is for sample use only, as Paediprime is under development
        phase only. The payment section is also experimental. So don't pay here.
      </div>
    </div>
  );
};

export default AlertSlider;
